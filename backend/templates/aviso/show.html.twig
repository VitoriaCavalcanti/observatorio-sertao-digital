{% extends 'base.html.twig' %}{% block title %}{{ post.titulo }}{% endblock %}{% block main %}<article class="post"><h1>{{ post.titulo }}</h1><p class="post-metadata"><twig:ux:icon name="tabler:calendar-month"/> {{ post.publicadoEm|date('d/m/Y H:i') }} · {{ post.autor.nome }}</p><p class="lead">{{ post.resumo }}</p><div>{{ post.conteudo|nl2br }}</div></article>{% endblock %}
