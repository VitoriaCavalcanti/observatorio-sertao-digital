{% extends 'base.html.twig' %}
{% block title %}{{ post.id ? 'Editar aviso' : 'Novo aviso' }}{% endblock %}
{% block body %}<div class="page-head"><div><p class="eyebrow">CENTRAL DE AVISOS</p><h1>{{ post.id ? 'Editar aviso' : 'Novo aviso' }}</h1></div></div><div class="panel form-panel">{{ form_start(form) }}{{ form_widget(form) }}<div class="form-actions"><button class="primary">Salvar</button><a href="{{ path('admin_post_index') }}">Cancelar</a></div>{{ form_end(form) }}</div>{% endblock %}
