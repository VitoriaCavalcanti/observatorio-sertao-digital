{% extends 'base.html.twig' %}{% block title %}Usuário{% endblock %}{% block body %}<div class="page-head"><div><p class="eyebrow">SEGURANÇA</p><h1>{{ usuario.id ? 'Editar usuário' : 'Novo usuário' }}</h1></div></div><div class="panel form-panel">{{ form_start(form) }}{{ form_widget(form) }}<div class="form-actions"><button class="primary">Salvar</button><a href="{{ path('admin_user_index') }}">Cancelar</a></div>{{ form_end(form) }}</div>{% endblock %}
