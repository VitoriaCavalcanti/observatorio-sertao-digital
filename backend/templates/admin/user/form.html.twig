{% extends 'admin/layout.html.twig' %}{% block title %}Usuário{% endblock %}{% block body %}<div class="page-head"><h1>{{ usuario.id ? 'Editar usuário' : 'Novo usuário' }}</h1></div><div class="panel form-panel">{{ form_start(form) }}{{ form_widget(form) }}<div class="form-actions"><button class="btn btn-primary">Salvar</button><a class="btn btn-link" href="{{ path('admin_user_index') }}">Cancelar</a></div>{{ form_end(form) }}</div>{% endblock %}
